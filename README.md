# Germane Media LLC — Murder Mystery

A browser-based 30-minute team murder mystery designed for Google Meet.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```

## Streamlit deployment

Push `app.py` and `requirements.txt` to GitHub, then deploy the repository using Streamlit Community Cloud.

No external API or database is required for this first version.
